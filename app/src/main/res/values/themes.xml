<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TriviaGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
<!--        <item name = "android:fontFamily">@font/anaphora</item>-->
    </style>
    
    <style name="customTextColor">
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="Theme.TriviaGame" parent="Base.Theme.TriviaGame"/>
    
    <style name="Theme.TriviaGame.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="textShadow" parent="customTextColor">
        <item name="android:shadowDx">-5</item>
        <item name="android:shadowDy">-5</item>
        <item name="android:shadowRadius">15</item>
        <item name="android:shadowColor">@color/black</item>
    </style>
</resources>